'use strict';

const assert = require('assert');
const EasyI18n = require('../src/easy-i18n');

describe('test/easy-i18n-cli2.test.js', () => {
  it('should be ok', () => {
    assert(EasyI18n);
  });
});
